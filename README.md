# AI_LABS — Лабораторные работы по курсу «Киберфизические системы»

Репозиторий содержит результаты лабораторных работ по заданиям из `Lab_tasks.pdf`:

- `AI_LAB1` — ЛР №1 (Computer Vision), вариант **на 3**: object detection на базе Ultralytics/YOLO + самостоятельная имплементация модели;
- `AI_Lab2` — ЛР №2 (NLP), вариант **на 4**: Docker + FastAPI + Ollama (`qwen2.5:0.5b`) для классификации SMS spam/ham.

## Структура репозитория

- `AI_LAB1/labCV.ipynb` — полный отчет и код по ЛР №1.
- `AI_Lab2/` — сервис и артефакты по ЛР №2.
- `AI_Lab2/README.md` — детальная инструкция и проверка ЛР №2.

## Лабораторная работа №1 (CV, вариант на 3)

### Соответствие требованиям задания

В `AI_LAB1/labCV.ipynb` выполнено:

- выбор и обоснование [датасета дорожных знаков/автомобилей](<https://www.kaggle.com/datasets/pkdarabi/cardetection>);
- выбор метрик `Precision`, `Recall`, `mAP@50`, `mAP@50:95`, `F1`;
- обучение бейзлайна (`YOLOv8n`) и его оценка;
- формулировка и проверка гипотез улучшения (аугментации, `Cosine LR`, изменение конфигурации обучения);
- обучение улучшенного бейзлайна и сравнение с базовым;
- самостоятельная имплементация детектора на PyTorch;
- добавление в собственную модель техник из улучшенного бейзлайна и повторная оценка;
- итоговые выводы по каждому этапу.

### Быстрый запуск ЛР1

1. Установить Python 3.10+.
3. Установить зависимости:

```bash
pip install ultralytics torch torchvision opencv-python matplotlib seaborn pandas scikit-learn jupyter
```

4. Открыть ноутбук:

```bash
jupyter notebook AI_LAB1/labCV.ipynb
```

5. Последовательно выполнить ячейки.

> Примечание: в ноутбуке путь к `data.yaml` задан абсолютным путем (`data_config = ...`). При запуске на другой машине замените его на актуальный локальный путь к датасету.

## Лабораторная работа №2 (вариант на 4)

Выполнение ЛР2 находится в директории `AI_Lab2/` и включает:

- контейнеризацию сервиса;
- запуск Ollama + модели `qwen2.5:0.5b`;
- FastAPI-обертку для инференса по HTTP;
- скрипты проверки сервиса.

Запуск (подробности в `AI_Lab2/README.md`):

```bash
cd AI_Lab2
docker compose up --build
```

## Воспроизводимость

- Для ЛР1 воспроизводимость обеспечивается ноутбуком `AI_LAB1/labCV.ipynb` с полным пайплайном обучения и оценки.
- Для ЛР2 воспроизводимость обеспечивается `Dockerfile`, `docker-compose.yml`, `start.sh` и тестовым скриптом.
